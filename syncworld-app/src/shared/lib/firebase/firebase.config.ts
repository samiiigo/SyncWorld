// ──────────────────────────────────────────────
// firebase.config.ts — Firebase SDK initialization
// SRP: SDK init only — no business logic
// ──────────────────────────────────────────────

/**
 * Initializes the Firebase app instance.
 *
 * TODO: import { initializeApp } from 'firebase/app';
 * TODO: read config from loadEnvConfig()
 * TODO: call initializeApp(config)
 * TODO: export the app instance for use by adapters
 * TODO: guard against double initialization
 */
export function initFirebase(): void {
  // TODO: implement
  throw new Error('initFirebase not implemented');
}
