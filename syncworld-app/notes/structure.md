# Structure

- `App.tsx` is the current placeholder entry screen.
- `index.ts` registers the Expo root component.
- `src/` contains the planned feature, service, store, config, and type layers.
- `docs/` contains product and architecture planning.
- `notes/` is the short reference folder for quick review.
