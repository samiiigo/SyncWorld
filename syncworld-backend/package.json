{
  "name": "syncworld-backend",
  "private": true,
  "workspaces": [
    "functions",
    "realtime-server"
  ],
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"jest firestore/firestore.rules.test.ts\"",
    "test": "npm run test --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "@types/jest": "^30.0.0",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.13.1",
    "jest": "^30.1.3",
    "ts-jest": "^29.4.1",
    "typescript": "^5.9.2"
  }
}
