# Workspace Scaffold

- Added `syncworld-backend/` as a separate backend workspace.
- Added root workspace config with `functions/` and `realtime-server/` subprojects.
- Added strict shared TypeScript base config.
- Added env validation modules for functions and realtime server.
- Added Firebase project wiring files.
- The backend typecheck and Firestore rules tests currently pass.
