# Functions

- Added `createRoom` callable.
- Added `joinRoom` callable.
- Added `transitionRoomStatus` callable.
- Added vote resolution trigger and proposal expiry sweep.
- Added alarm firing and orphan cleanup scheduled jobs.
- Added shared FSM, validation, and rate limit helpers.
